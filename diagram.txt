zsh opens -> ~/.oh-my-zsh/custom/load_bash_profile.zsh -> ~/.bash_profile (linked)

nvim opens -> ~/.config/.../init.vim (linked) -> ~/.config/.../coc/vim (linked)

tmux opens -> ~/.tmux.conf (linked)
